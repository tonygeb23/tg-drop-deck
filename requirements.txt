wxPython
numpy
sounddevice
soundfile
soxr
accessible_output2
# The MPEG-4 family, which libsndfile does not open: m4a, m4b, aac, wma, opus.
# PyAV ships FFmpeg with it, so there is no separate binary to install.
av
# Artist and title out of the file's own tags. Pure Python, decodes nothing.
mutagen
# Compressor, gate, equaliser and limiter, and the VST3 host behind
# the voice tab. Without it the microphone goes out unprocessed.
pedalboard
