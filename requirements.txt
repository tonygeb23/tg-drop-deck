wxPython
numpy
sounddevice
soundfile
soxr
accessible_output2
# The MPEG-4 family, which libsndfile does not open: m4a, m4b, aac, wma, opus.
# PyAV ships FFmpeg with it, so there is no separate binary to install.
av
# Artist and title out of the file's own tags. Pure Python, decodes nothing.
mutagen
# Compressor, gate, equaliser and limiter, and the VST3 host behind
# the voice tab. Without it the microphone goes out unprocessed.
pedalboard
# Telling a presenter what the camera can see: whether they are in shot,
# centred and lit. Headless because the normal wheel drags in Qt for GUI
# windows this app never opens. Without it the framing announcements turn
# themselves off and say so; streaming is unaffected.
opencv-python-headless
# Real text on the picture: station name, what is playing, a clock. The only
# thing in the bundle that can put a glyph on a pixel. cv2 has the Hershey
# stroke fonts and nothing else, numpy has no rasteriser, and the FFmpeg PyAV
# ships was built with no libfreetype, so drawtext is not there either.
# Measured 8 September 2026. About 3 MB once _avif is excluded at build time.
Pillow
# Dark mode that does not cost a control its screen reader role. It points
# real native controls at the Windows dark theme classes rather than letting
# wxMSW owner-draw them, which is what turns every check box into a "button"
# with no checked state. Without it the window stays light and nothing says
# why, so the selftest themes a real window and reads the colour back.
git+https://github.com/alexoloopios/accessible-wxpython-darkmode.git
